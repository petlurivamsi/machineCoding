app.get("/", () => {
  //call database and it returns data
  //try{
  //const dbcall = await dbCall();
  //logger.info('Database called successfully')
  // }catch(ex){
  // logger.warn('DB CALL failed')
  //handle error
  // }
});
